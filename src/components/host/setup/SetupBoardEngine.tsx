/**
 * Thin setup board shell backed by the trip-engine.
 * Server is source of truth — no sessionStorage draft overlay.
 */
export { SetupBoardClient as SetupBoardEngine } from "./SetupBoardClient";
