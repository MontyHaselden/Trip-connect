/**
 * @deprecated Legacy wizard shadow itinerary_items for publish compatibility.
 * Dashboard1 setup engine does NOT write shadow items.
 * Only invoke from explicit publish migration if still required.
 */
export const LEGACY_SHADOW_ITEMS_ENABLED = false;
